---
import Logotype from '@/components/Logotype.astro'
// @ts-ignore
import { menu, links } from '@/data/informations'
import InfoAside from './InfoAside.astro'
---

<div class="flex lg:hidden mb-6 items-center gap-1 justify-between">
  <Logotype />
  <button id="open" class="px-3 py-1 rounded-lg bg-gray-50 ring-1 ring-gray-100"
    >Меню</button
  >
  <nav
    id="menu"
    class="p-6 z-20 w-full bg-black/70 backdrop-blur-lg text-white absolute left-0 h-full top-[85px] hidden flex flex-col gap-3"
  >
    {
      menu.map((item) => (
        <a class="font-medium text-xl" href={item.link}>
          &#10149; {item.name}
        </a>
      ))
    }
    <div class="mt-8 flex flex-col gap-6">
      <p class=" font-semibold">💛 При поддержке:</p>
      <ul class="flex flex-wrap gap-2">
        {
          links.map((item) => (
            <li class="px-3 py-1.5 font-medium rounded-md bg-black/50 hover:bg-black text-white duration-75 ease-linear transition-colors">
              <a href={item.link}>{item.name}</a>
            </li>
          ))
        }
      </ul>
      <InfoAside />
    </div>
  </nav>
</div>

<script is:inline>
  const menu = document.querySelector('#menu')
  const open = document.querySelector('#open')

  open.addEventListener('click', () => {
    menu.classList.toggle('hidden')
  })
</script>
